package ase.rest.session;

public class SessionController {
    //service, DTOmapper

    //constructor

    //post(/"startSession") @Body SessionDTO {Sessionservice.create(ActivityDTO.apply(activity))}
}
